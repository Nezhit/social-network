server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: social_network
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.yaml
    enabled: true
  datasource:
    driver-class-name: org.postgresql.Driver